discord-banking-bot/
├── index.js       ← bot startup & command router
├── banking.js     ← all commands (already downloaded)
├── db.js          ← postgresql database functions
├── package.json   ← libraries list
└── .gitignore     ← keeps secrets off GitHub
